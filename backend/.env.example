# Port the backend server runs on
PORT=5000

# Comma-separated list of allowed frontend origins (CORS)
CLIENT_ORIGIN=http://localhost:3000

# Path to the JSON data file (created automatically if missing)
DB_PATH=./data/chat.json
